import string 
import random 
print("Welcome to Password Generator")
a=int(input("Enter the desired length"))
password=""
b=string.digits + string.ascii_letters + string.punctuation
for i in range(a):
    password+=random.choice(b)
print("Password generated ",password)
